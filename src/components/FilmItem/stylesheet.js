import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    filmItem: {
        borderRadius: 5,
        backgroundColor: '#eee',
        padding: 10,
        marginTop: 5,
        marginBottom: 5
    }
});